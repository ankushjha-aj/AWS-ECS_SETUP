name: Build and Push
on:
  pull_request:
    branches: ['main']
permissions:
  id-token: write
  contents: read
  
jobs:
  build_backend:
    uses: ankushjha-aj/AWS-ECS_SETUP/.github/workflows/Reusable.yml@develop-1
    with:
      dockerfile_path: Dockerfile
      aws_region: us-east-1
      docker_push: true
      ecr_repository_uri: 975050300138.dkr.ecr.us-east-1.amazonaws.com/test
